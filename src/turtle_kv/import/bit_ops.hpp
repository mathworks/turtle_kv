#pragma once

#include <batteries/bit_ops.hpp>

namespace turtle_kv {

using batt::bit_count;
using batt::bit_rank;
using batt::bit_select;
using batt::first_bit;
using batt::get_bit;
using batt::insert_bit;
using batt::last_bit;
using batt::mask_from_interval;
using batt::next_bit;
using batt::prev_bit;
using batt::remove_bit;
using batt::set_bit;

}  // namespace turtle_kv
